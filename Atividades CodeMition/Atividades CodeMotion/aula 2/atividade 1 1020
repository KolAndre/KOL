#include <stdio.h>

int main () {
	int dia, mes, ano, entrada, aux;
	
	scanf ("%d", &entrada);
	
	ano = entrada/365;
	
	aux = entrada%365;
	
	dia = aux%30;
	
	mes = aux/30;
	
	printf ("%2d ano(s)\n %d mes(ses)\n %d dia(s)",ano, mes, dia);
	
	return 0;
}
